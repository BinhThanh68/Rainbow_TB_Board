#include "barcode_scanner.h"
#include <string.h>
#include <stdio.h>
#include "system_config/default/system_config.h"


char data; char dataarray[50];
int ind,test;
void Init_Scanner()
{  
   //INIT_UART_2(4800, 1, 0);
//   char buffer[50];
//    int a = 10, b = 20, c;
//    c = a + b;
//    sprintf(buffer, "Sum of %d and %d is %d", a, b, c);
// 
//    // The string "sum of 10 and 20 is 30" is stored
//    // into buffer instead of printing on stdout
//    printf("%s", buffer);
//    ind =0;
//    test =0;
}
 
void Read_Scanner_Data()
{
//Test UART transmit
//    float fdata = 123.45;e
//    char dataa[20];
//    sprintf(dataa, "%.2f\n", fdata);
//    WriteUART2(dataa, strlen(dataa)); 
//    int i;
//    for(i=0;i<6000000;i++);
}
